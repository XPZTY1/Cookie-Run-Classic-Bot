# ---------------------------------------------------------------------------
# PAUSE_EVENTS: เหตุการณ์ที่ต้อง "หยุดทำงานชั่วคราวทั้งหมด" + แจ้งเตือน LINE
# ---------------------------------------------------------------------------
# พฤติกรรม:
#   1) ทุกรอบของ bot_loop จะเช็คก่อนเป็นอันดับแรก (ก่อน INTERRUPTS และก่อน FLOW)
#   2) ถ้าเจอ template ตัวใดตัวหนึ่งในนี้ครั้งแรก ->
#        - เซฟภาพหน้าจอไว้ที่โฟลเดอร์ pause_screenshots/
#        - ให้ Gemini อ่านไฟล์ภาพที่เพิ่งเซฟไว้ แล้วบรรยายว่าหน้าจอตอนนั้นแสดงอะไร
#          (เพื่อแจ้งเตือนเฉยๆ ไม่ได้ใช้ตัดสินใจกดอะไร)
#        - ส่งข้อความ + คำบรรยายเข้า LINE 1 ครั้ง
#        - เข้าสู่สถานะ "pause" (ไม่กด ไม่ทำ action ใดๆ รอเฉยๆ ให้ผู้ใช้ไปจัดการเอง)
#   3) วนเช็คซ้ำเรื่อยๆ ตราบใดที่ยังเจอ template นั้นอยู่บนจอ ก็ยังคง pause ต่อไป
#        (ไม่สแปมข้อความ LINE ซ้ำ แต่ยังเซฟภาพใหม่ทุกรอบด้วยชื่อไฟล์ที่มี timestamp
#        ของตัวเอง เพื่อเก็บบันทึกความเปลี่ยนแปลงของหน้าจอไว้เรื่อยๆ)
#   4) เมื่อ template นั้นหายไปจากจอ (ผู้ใช้จัดการเสร็จแล้ว) ->
#        - ส่งข้อความแจ้งว่ากลับมาทำงานแล้ว
#        - กลับไปทำงานตาม FLOW ต่อจาก state เดิมที่ค้างอยู่ก่อนโดนหยุด (current_state ไม่ถูกแตะต้องเลย)
PAUSE_EVENTS = {
    "connection_error": {
        "template": "connection_error.png",
        "message": "⚠️ บอทหยุดทำงานชั่วคราว: เจอหน้าจอ Connection Error",
        "resume_message": "✅ บอทกลับมาทำงานต่อแล้ว (Connection Error หายไปแล้ว)",
        "check_interval": 3,
    },
    "verification_screen": {
        "template": "verification_screen.png",
        "message": "⚠️ บอทหยุดทำงานชั่วคราว: เจอหน้าจอที่ต้องยืนยันตัวตน กรุณาเข้าไปทำเองในเกม",
        "resume_message": "✅ บอทกลับมาทำงานต่อแล้ว (หน้ายืนยันตัวตนหายไปแล้ว)",
        "check_interval": 3,
    },
    # เพิ่ม event อื่นๆ ที่ต้องการให้บอทหยุดรอ+แจ้งเตือนได้ที่นี่
}
